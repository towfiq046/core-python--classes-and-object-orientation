class Rectangle:

    def __init__(self, width, height):
        # Instance attributes are assigned on per object basis.
        # Each object has its own instance attribute.
        self.width = width
        self.height = height
